#include "main.h"
/**
 * print_alphabet - prints the alphabet, in lowercase
 * Return: Returns nothing
 */
void print_alphabet(void)
{
	int a;

	for (a = 97; a < 123; a++)
		_putchar(a);
	_putchar('\n');
}
