#include "main.h"
/**
 * add - adds two integers and return the results
 * @a: first number
 * @b: second number
 * Return: Sum of a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
